<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.teamer.teapot.ai.core.dao.MCPConfigMapper">

    <resultMap id="mcpMap" type="com.teamer.teapot.ai.core.model.MCPConfigDO">
        <id property="id" column="id"/>
        <result property="name" column="name"/>
        <result property="transport" column="transport"/>
        <result property="command" column="command"/>
        <result property="args" column="args"/>
        <result property="env" column="env"/>
        <result property="url" column="url"/>
        <result property="headers" column="headers"/>
        <result property="enabled" column="enabled"/>
        <result property="description" column="description"/>
        <result property="remark" column="remark"/>
        <result property="updatedBy" column="updated_by"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <sql id="allColumns">
        id, name, transport, command, args, env, url, headers,
        enabled, description, remark, updated_by, created_at, updated_at
    </sql>

    <select id="selectAll" resultMap="mcpMap">
        SELECT <include refid="allColumns"/> FROM t_mcp_config ORDER BY id
    </select>

    <select id="selectByName" resultMap="mcpMap">
        SELECT <include refid="allColumns"/> FROM t_mcp_config WHERE name = #{name} LIMIT 1
    </select>

    <insert id="insert">
        INSERT INTO t_mcp_config
            (name, transport, command, args, env, url, headers, enabled, description, remark, updated_by)
        VALUES
            (#{name}, #{transport}, #{command}, #{args}, #{env}, #{url}, #{headers}, #{enabled}, #{description}, #{remark}, #{updatedBy})
    </insert>

    <update id="updateByName">
        UPDATE t_mcp_config
        <set>
            <if test="transport != null">transport = #{transport},</if>
            <if test="command != null">command = #{command},</if>
            <if test="args != null">args = #{args},</if>
            <if test="env != null">env = #{env},</if>
            <if test="url != null">url = #{url},</if>
            <if test="headers != null">headers = #{headers},</if>
            <if test="enabled != null">enabled = #{enabled},</if>
            <if test="description != null">description = #{description},</if>
            <if test="remark != null">remark = #{remark},</if>
            updated_by = #{updatedBy}
        </set>
        WHERE name = #{name}
    </update>

    <delete id="deleteByName">
        DELETE FROM t_mcp_config WHERE name = #{name}
    </delete>

    <update id="toggleEnabled">
        UPDATE t_mcp_config SET enabled = #{enabled}, updated_by = #{updatedBy} WHERE name = #{name}
    </update>
</mapper>
