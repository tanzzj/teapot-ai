<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>com.teamer.teapot</groupId>
        <artifactId>teapot-ai-server</artifactId>
        <version>1.0.0-SNAPSHOT</version>
    </parent>

    <artifactId>teapot-ai-core</artifactId>
    <name>teapot-ai-core</name>
    <description>核心业务模块（SPEC §4/§6/§7）：Agent / Skill 管理服务 + AgentRegistry + AG-UI 接入</description>

    <dependencies>
        <dependency>
            <groupId>com.teamer.teapot</groupId>
            <artifactId>teapot-ai-rbac</artifactId>
        </dependency>

        <!-- AgentScope 核心（HarnessAgent，传递依赖 agentscope-core） -->
        <dependency>
            <groupId>io.agentscope</groupId>
            <artifactId>agentscope-harness</artifactId>
        </dependency>
        <!-- 模型接入：一期 DashScope + OpenAI 双供应商 -->
        <dependency>
            <groupId>io.agentscope</groupId>
            <artifactId>agentscope-extensions-model-dashscope</artifactId>
        </dependency>
        <dependency>
            <groupId>io.agentscope</groupId>
            <artifactId>agentscope-extensions-model-openai</artifactId>
        </dependency>
        <!-- MySQL 会话状态存储（MysqlAgentStateStore / JdbcStore） -->
        <dependency>
            <groupId>io.agentscope</groupId>
            <artifactId>agentscope-extensions-mysql</artifactId>
        </dependency>
        <!-- MySQL Skill 仓库 -->
        <dependency>
            <groupId>io.agentscope</groupId>
            <artifactId>agentscope-extensions-skill-mysql-repository</artifactId>
        </dependency>
        <!-- Git Skill 仓库（SPEC §15：第二 skill 来源，只读） -->
        <dependency>
            <groupId>io.agentscope</groupId>
            <artifactId>agentscope-extensions-skill-git-repository</artifactId>
        </dependency>
        <!-- 阿里云 AgentRun 沙箱（SPEC §16：按 Agent feature 启用） -->
        <dependency>
            <groupId>io.agentscope</groupId>
            <artifactId>agentscope-extensions-sandbox-agentrun</artifactId>
        </dependency>
        <!-- E2B 兼容沙箱（AgentRun E2B 端点，配置后优先于 AgentRun MCP 链路） -->
        <dependency>
            <groupId>io.agentscope</groupId>
            <artifactId>agentscope-extensions-sandbox-e2b</artifactId>
        </dependency>
        <!-- AG-UI Spring Boot Starter（SSE 端点、多 Agent 路由、HITL） -->
        <dependency>
            <groupId>io.agentscope</groupId>
            <artifactId>agentscope-agui-spring-boot-starter</artifactId>
        </dependency>

        <dependency>
            <groupId>com.github.ben-manes.caffeine</groupId>
            <artifactId>caffeine</artifactId>
        </dependency>
        <!-- 阿里云 OSS Java SDK V2（SPEC §20：图片存储 oss 策略，preview 期 bom 锁版） -->
        <dependency>
            <groupId>com.aliyun</groupId>
            <artifactId>alibabacloud-oss-v2</artifactId>
        </dependency>
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <scope>provided</scope>
        </dependency>
    </dependencies>
</project>
